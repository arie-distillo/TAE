add detailed logging at each step
what Jetson Nano to purchase - dev kit? 
skip ingestion when it's already done
Multi-Angle Persistence - how is it manifesetd in top 3 candidates?
Prompt `f"Locate all instances of '{user_query}' in these images: {filenames_str}\n"` - handle cases of a different type of query, beyond finding a specific object
sometimes model returns bad "bbox" in "targets" like `[0, 0, 1000, 600] [1000, 600, 2000, 1200] [2000, 0, 3000, 600]` need to detect such casees and retry